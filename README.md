# plex-docker
Plex Media Server Docker Files

This is the docker-compose file that creates container for plex services.  

Creates a Plex container with all ports open
Creates a dizquetv container for creating live channels

Will need to change media volume on linux
